name: QA TCHA

on:
  workflow_dispatch:
  pull_request:
    paths:
      - '.github/workflows/qa-tcha.yml'
      - 'backend/**'
      - 'frontend/**'
      - 'DOCUMENTACAO/AI/**'
  push:
    branches:
      - main

jobs:
  qa-tcha:
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: Install backend dependencies
        working-directory: backend
        run: npm ci

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Audit backend production dependencies
        working-directory: backend
        run: npm audit --omit=dev --audit-level=high

      - name: Audit frontend production dependencies
        working-directory: frontend
        run: npm audit --omit=dev --audit-level=high

      - name: Build backend
        working-directory: backend
        run: npm run build

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Validate attendance notifications
        working-directory: backend
        run: npm run test:attendance-notifications

      - name: Validate security containment
        working-directory: backend
        run: node test/security-containment.test.cjs

      - name: Validate Central identity and Financeiro contracts
        working-directory: backend
        run: |
          node test/central-identity-session-security.test.cjs
          node test/financeiro-hmac-contract.test.cjs

      - name: Validate Telegram security and PostgreSQL grants
        working-directory: backend
        run: |
          node test/telegram-webhook-security.test.cjs
          node test/postgresql-runtime-grants.test.cjs
          npm run prisma:postgres:validate

      - name: Validate cookie-only frontend authentication
        working-directory: frontend
        run: npm run test:cookie-auth

      - name: Install Playwright browser
        working-directory: frontend
        run: npx playwright install --with-deps chromium

      - name: Run E2E regression
        working-directory: frontend
        run: npm run test:e2e

      - name: Upload Playwright artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qa-tcha-artifacts
          path: |
            frontend/playwright-report
            frontend/test-results
            frontend/tests/e2e/.artifacts
