'use client';

import RecordStatusIndicator from '@/app/components/record-status-indicator';

export default function InactiveRecordIndicator() {
    return <RecordStatusIndicator active={false} />;
}
